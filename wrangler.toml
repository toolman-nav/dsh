name = "dsh"
compatibility_date = "2026-08-21"

# Publish the Vite build plus SEO/GEO prerender output, not the legacy design snapshot.
[assets]
directory = "./frontend/dist"
not_found_handling = "404-page"
html_handling = "auto-trailing-slash"
