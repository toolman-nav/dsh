name = "dsh"
compatibility_date = "2024-11-11"

# Cloudflare Git 实际跑的是 `npx wrangler deploy`（不是 pages deploy）。
# pages_build_output_dir 只给 `wrangler pages deploy` 用，会被忽略并报 Missing entry-point。
[assets]
directory = "./design"
not_found_handling = "single-page-application"
