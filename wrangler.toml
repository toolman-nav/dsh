name = "dsh"
compatibility_date = "2026-08-21"

# Git 接到的是 Workers Builds（检查名 Workers Builds: dsh），不是 Pages。
# 静态 Vue 包在 design/，与本地 5173 同一套。
[assets]
directory = "./design"
not_found_handling = "single-page-application"
