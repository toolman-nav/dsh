{
  "name": "dsh",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('static assets already in design/')\"",
    "deploy": "wrangler pages deploy ./design --project-name dsh"
  }
}
